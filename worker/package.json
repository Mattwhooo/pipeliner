{
  "name": "pipeliner-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Pipeliner reference worker — claims steps and drives Claude Code (docs/worker.md)",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
