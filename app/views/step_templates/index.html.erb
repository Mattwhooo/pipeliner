<% content_for :title, "Step Library · Pipeliner" %>

<div class="flex items-center justify-between">
  <div>
    <h1 class="text-2xl font-semibold text-default">Step Library</h1>
    <p class="mt-1 text-sm text-muted">Reusable step templates you can compose into workflows.</p>
  </div>
  <%= link_to "New template", new_step_template_path,
        class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-700" %>
</div>

<% grouped = @templates.group_by(&:phase) %>
<% sections = Phase::KINDS_IN_ORDER.map { |k| [ k, k.humanize ] } + [ [ nil, "Any" ] ] %>

<div class="mt-8 space-y-8">
  <% sections.each do |phase_key, heading| %>
    <% templates = grouped[phase_key] %>
    <% next if templates.blank? %>
    <section>
      <h2 class="text-lg font-semibold text-default"><%= heading %></h2>
      <% if phase_key == "define" %>
        <p class="mt-1 text-sm text-muted">
          Define runs the same core for every pipeline; custom Define steps run late in the phase.
        </p>
      <% end %>
      <div class="mt-3 overflow-hidden rounded-lg border border-default bg-surface shadow-sm">
        <table class="min-w-full divide-y divide-default">
          <thead>
            <tr>
              <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Name</th>
              <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Type</th>
              <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Role</th>
              <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Scope</th>
              <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Usage</th>
              <th class="px-6 py-3"></th>
            </tr>
          </thead>
          <tbody class="divide-y divide-default">
            <% templates.each do |template| %>
              <tr class="hover:bg-surface-hover">
                <td class="px-6 py-4">
                  <div class="text-sm font-medium text-default"><%= template.name %></div>
                  <div class="mt-0.5 line-clamp-1 text-xs text-muted"><%= template.system_prompt %></div>
                </td>
                <td class="px-6 py-4">
                  <span class="rounded bg-surface-hover px-1.5 py-0.5 text-xs font-medium text-muted"><%= template.step_type %></span>
                </td>
                <td class="px-6 py-4 font-mono text-xs text-muted"><%= template.role %></td>
                <td class="px-6 py-4 text-xs text-muted">
                  <%= template.project ? template.project.name : "Global" %>
                </td>
                <td class="px-6 py-4 text-xs text-muted"><%= template.requirement %></td>
                <td class="px-6 py-4 text-right text-sm">
                  <%= link_to "Edit", edit_step_template_path(template),
                        class: "font-medium text-indigo-600 hover:text-indigo-700" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </section>
  <% end %>

  <% if @templates.empty? %>
    <div class="rounded-lg border border-dashed border-default bg-surface px-6 py-12 text-center">
      <p class="text-sm text-muted">No templates yet.</p>
      <%= link_to "New template", new_step_template_path,
            class: "mt-3 inline-block rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-700" %>
    </div>
  <% end %>
</div>
