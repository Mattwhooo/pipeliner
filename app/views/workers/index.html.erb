<% content_for :title, "Workers · Pipeliner" %>

<div class="flex items-center justify-between">
  <h1 class="text-2xl font-semibold text-default">Workers</h1>
</div>

<% if @workers.any? %>
  <div class="mt-8 overflow-hidden rounded-lg border border-default bg-surface shadow-sm">
    <table class="min-w-full divide-y divide-default">
      <thead>
        <tr>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Worker</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Status</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Backend</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Roles</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Concurrency</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-muted">Last heartbeat</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-default">
        <% @workers.each do |worker| %>
          <tr class="hover:bg-surface-hover">
            <td class="px-6 py-4">
              <div class="text-sm font-medium text-default"><%= worker.name.presence || worker.public_id %></div>
              <div class="font-mono text-xs text-muted"><%= worker.public_id %></div>
            </td>
            <td class="px-6 py-4"><%= status_badge(worker.status) %></td>
            <td class="px-6 py-4 text-sm text-muted"><%= worker.backend %></td>
            <td class="px-6 py-4">
              <div class="flex flex-wrap gap-1">
                <% worker.supported_roles.each do |role| %>
                  <span class="rounded bg-surface-hover px-1.5 py-0.5 font-mono text-xs text-muted"><%= role %></span>
                <% end %>
              </div>
            </td>
            <td class="px-6 py-4 text-sm text-muted"><%= worker.concurrency %></td>
            <td class="px-6 py-4 text-sm text-muted">
              <% if worker.last_heartbeat_at %>
                <time title="<%= worker.last_heartbeat_at %>"><%= time_ago_in_words(worker.last_heartbeat_at) %> ago</time>
              <% else %>
                —
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div class="mt-8 rounded-lg border border-default bg-surface p-6 shadow-sm">
    <div class="py-12 text-center">
      <p class="text-sm font-medium text-default">No workers registered</p>
      <p class="mt-1 text-xs text-muted">
        Start a worker and it will self-register here. See <span class="font-mono">worker/README.md</span>.
      </p>
    </div>
  </div>
<% end %>
